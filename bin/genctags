#!/bin/bash

if [ -f $PWD/.gitignore ]; then
  exclude="--exclude=@${PWD}/.gitignore"
fi

loc=$1
if [ -z $loc ]; then
  loc="${PWd}"
fi

ctags -R $loc --sort=yes --languages=+Python,+JavaScript --verbose=yes $exclude
